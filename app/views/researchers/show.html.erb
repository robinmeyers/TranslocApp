<% provide(:title, @researcher.name) %>
<div class="row">
  <aside class="span2">
    <section>
      <h1>
        <%= gravatar_for @researcher %>
        <%= @researcher.name %>
      </h1>
    </section>
  </aside>
  <div class="span10">
    <% if @experiments.any? %>
      <h3>Experiments (<%= @experiments.count %>)</h3>
      <table class="table table-hover table-condensed">
        <thead>
          <tr>
            <th>Name</th>
            <th>Run</th>
            <th style="width: 30%">Description</th>
            <th>Genome</th>
            <th>BreakSite</th>
            <th>Cutter</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <%= render partial: 'experiments/researcher_experiment',
            collection: @experiments, as: 'experiment' %>
        </tbody>
      </table>
      <%= will_paginate @experiments %>
    <% end %>
  </div>
</div>