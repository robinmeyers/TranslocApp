<% provide(:title, @researcher.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @researcher %>
        <%= @researcher.name %>
      </h1>
    </section>
  </aside>
  <div class="span8">
    <% if @researcher.experiments.any? %>
      <h3>Experiments (<%= @researcher.experiments.count %>)</h3>
      <ol class="experiments">
        <%= render @experiments %>
      </ol>
      <%= will_paginate @experiments %>
    <% end %>
  </div>
</div>